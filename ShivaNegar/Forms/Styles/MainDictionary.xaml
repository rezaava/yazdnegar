<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:ShivaNegar.Forms.Controls"
    xmlns:converters="clr-namespace:ShivaNegar.Forms.Converters"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:sys="clr-namespace:System;assembly=mscorlib">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Light.xaml" />
        <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Defaults.xaml" />
        <materialDesign:BundledTheme
            BaseTheme="Light"
            PrimaryColor="LightBlue"
            SecondaryColor="Green" />

    </ResourceDictionary.MergedDictionaries>


    <sys:String x:Key="FormBorderRadius">10</sys:String>

    <SolidColorBrush x:Key="LightBackgroundColor" Color="White" />
    <SolidColorBrush x:Key="ExitBackgroundButton" Color="#F40606" />

    <SolidColorBrush x:Key="PrimaryBackgroundColor" Color="#06aef4" />
    <SolidColorBrush x:Key="SecondaryBackgroundColor" Color="#FF007AC1" />
    <SolidColorBrush x:Key="LightTextColor" Color="Black" />

    <SolidColorBrush x:Key="SelectedButtonBackground" Color="#212529" />

    <SolidColorBrush x:Key="LightButtonTextColor" Color="White" />
    <SolidColorBrush x:Key="ButtonTextColor" Color="#06aef4" />
    <SolidColorBrush x:Key="ImageColor" Color="#06aef4" />
    <SolidColorBrush x:Key="RippleButtonPrimary" Color="#aa06aef4" />

    <converters:IconToImageSourceConverter x:Key="IconToImageSourceConverter" />
    <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    <converters:StringToVisibilityConverter x:Key="StringToVisibilityConverter" />
    <converters:InvertBooleanToVisibilityConverter x:Key="InvertBooleanToVisibilityConverter" />

    <converters:IsLastItemConverter x:Key="IsLastItemConverter" />
    <converters:IsProgressedConverter x:Key="IsProgressedConverter" />
    <converters:BooleanToBackgroundConverter x:Key="BooleanToBackgroundConverter" />
    <converters:BooleanToHorizontalAlignmentConverter x:Key="BooleanToHorizontalAlignmentConverter" />

    <converters:BitmapToImageSourceConverter x:Key="BitmapToImageSourceConverter" />

    <LinearGradientBrush x:Key="wizardBarBrush" StartPoint="0.5,0.0" EndPoint="0.5,1.0">
        <GradientStop Offset="0.25" Color="#FFE4E4E4" />
        <GradientStop Offset="0.50" Color="#FFededed" />
        <GradientStop Offset="0.75" Color="#FFFCFCFC" />
    </LinearGradientBrush>

    <Style TargetType="{x:Type controls:WizardProgressBar}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:WizardProgressBar}">
                    <Border
                        Padding="{TemplateBinding Padding}"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        SnapsToDevicePixels="true"
                        UseLayoutRounding="True">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <ItemsControl ItemsSource="{TemplateBinding ItemsSource}" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <UniformGrid Rows="1">
                                            <UniformGrid.Effect>
                                                <DropShadowEffect
                                                    BlurRadius="3"
                                                    Opacity="0.6"
                                                    ShadowDepth="0"
                                                    Color="Black" />
                                            </UniformGrid.Effect>
                                        </UniformGrid>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <DataTemplate.Resources>
                                            <Style x:Key="outerPath" TargetType="Path">
                                                <Setter Property="Data" Value="M0.0,0.0 L0.0,0.33 L1.0,0.33 L1.0,0.66 L0.0,0.66 L0.0,1.0" />
                                                <Setter Property="StrokeThickness" Value="0" />
                                                <Setter Property="Height" Value="20" />
                                                <Setter Property="Stretch" Value="Fill" />
                                                <!--<Setter Property="Fill" Value="{StaticResource wizardBarBrush}" />-->
                                                <Setter Property="Fill" Value="{StaticResource SecondaryBackgroundColor}" />
                                                <Setter Property="StrokeEndLineCap" Value="Square" />
                                                <Setter Property="StrokeStartLineCap" Value="Square" />
                                                <Setter Property="Stroke" Value="Transparent" />
                                            </Style>
                                            <Style
                                                x:Key="innerPath"
                                                BasedOn="{StaticResource outerPath}"
                                                TargetType="Path">
                                                <Setter Property="Data" Value="M0.0,0.0 L0.0,0.45 L1.0,0.45 L1.0,0.55 L0.0,0.55 L0.0,1.0" />
                                                <Setter Property="Fill" Value="{Binding RelativeSource={RelativeSource AncestorType={x:Type controls:WizardProgressBar}}, Path=Foreground}" />
                                            </Style>
                                        </DataTemplate.Resources>
                                        <Grid SnapsToDevicePixels="True">
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto" />
                                                <RowDefinition Height="Auto" />
                                            </Grid.RowDefinitions>
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition />
                                                    <ColumnDefinition />
                                                </Grid.ColumnDefinitions>
                                                <Path Name="leftPath" Style="{StaticResource outerPath}" />
                                                <Path
                                                    Name="rightPath"
                                                    Grid.Column="1"
                                                    Style="{StaticResource outerPath}" />
                                                <Ellipse
                                                    Grid.ColumnSpan="2"
                                                    Width="20"
                                                    Height="20"
                                                    HorizontalAlignment="Center"
                                                    Fill="{StaticResource SecondaryBackgroundColor}"
                                                    Stroke="Transparent" />
                                                <Ellipse
                                                    Grid.ColumnSpan="2"
                                                    Width="14"
                                                    Height="14"
                                                    HorizontalAlignment="Center"
                                                    Fill="{Binding RelativeSource={RelativeSource AncestorType={x:Type controls:WizardProgressBar}}, Path=Foreground}"
                                                    Stroke="Transparent">
                                                    <Ellipse.Visibility>
                                                        <MultiBinding Converter="{StaticResource IsProgressedConverter}" ConverterParameter="False">
                                                            <Binding RelativeSource="{RelativeSource TemplatedParent}" />
                                                            <Binding Path="Progress" RelativeSource="{RelativeSource AncestorType={x:Type controls:WizardProgressBar}}" />
                                                        </MultiBinding>
                                                    </Ellipse.Visibility>
                                                </Ellipse>
                                                <Path
                                                    Name="leftFillPath"
                                                    Grid.Column="0"
                                                    Style="{StaticResource innerPath}">
                                                    <Path.Visibility>
                                                        <MultiBinding Converter="{StaticResource IsProgressedConverter}" ConverterParameter="False">
                                                            <Binding RelativeSource="{RelativeSource TemplatedParent}" />
                                                            <Binding Path="Progress" RelativeSource="{RelativeSource AncestorType={x:Type controls:WizardProgressBar}}" />
                                                        </MultiBinding>
                                                    </Path.Visibility>
                                                </Path>
                                                <Path
                                                    Name="rightFillPath"
                                                    Grid.Column="1"
                                                    Style="{StaticResource innerPath}">
                                                    <Path.Visibility>
                                                        <MultiBinding Converter="{StaticResource IsProgressedConverter}" ConverterParameter="True">
                                                            <Binding RelativeSource="{RelativeSource TemplatedParent}" />
                                                            <Binding Path="Progress" RelativeSource="{RelativeSource AncestorType={x:Type controls:WizardProgressBar}}" />
                                                        </MultiBinding>
                                                    </Path.Visibility>
                                                </Path>
                                            </Grid>
                                        </Grid>
                                        <DataTemplate.Triggers>
                                            <DataTrigger Binding="{Binding RelativeSource={RelativeSource PreviousData}}" Value="{x:Null}">
                                                <Setter TargetName="leftPath" Property="Visibility" Value="Collapsed" />
                                                <Setter TargetName="leftFillPath" Property="Visibility" Value="Collapsed" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self}, Converter={StaticResource IsLastItemConverter}}" Value="True">
                                                <Setter TargetName="rightPath" Property="Visibility" Value="Collapsed" />
                                                <Setter TargetName="rightFillPath" Property="Visibility" Value="Collapsed" />
                                            </DataTrigger>
                                        </DataTemplate.Triggers>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            <ItemsControl
                                Grid.Row="1"
                                ItemsSource="{TemplateBinding ItemsSource}"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <UniformGrid Rows="1" />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock
                                            Grid.Row="1"
                                            Grid.ColumnSpan="2"
                                            Margin="0,5,0,0"
                                            HorizontalAlignment="Center"
                                            FontWeight="Bold"
                                            Text="{Binding}" />
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--<SolidColorBrush x:Key="ProgressBarBorderBrush" Color="Transparent" />
	<SolidColorBrush
		x:Key="ProgressBarBackgroundBrush"
		Color="White" />
	<SolidColorBrush
		x:Key="ProgressBarTrackBackgroundBrush"
		Color="#63D055" />

	<Style
		x:Key="{x:Type ProgressBar}"
		TargetType="{x:Type ProgressBar}">
		<Setter
			Property="Template">
			<Setter.Value>
				<ControlTemplate
					TargetType="{x:Type ProgressBar}">
					<local:ClippingBorder
						x:Name="BorderBackground"
						CornerRadius="3"
						BorderThickness="0"
						BorderBrush="{StaticResource ProgressBarBorderBrush}"
						Background="{StaticResource ProgressBarBackgroundBrush}">
						<Grid>
							<VisualStateManager.VisualStateGroups>
								<VisualStateGroup
									x:Name="CommonStates">
									<VisualState
										x:Name="Determinate" />
									<VisualState
										x:Name="Indeterminate" />
								</VisualStateGroup>
							</VisualStateManager.VisualStateGroups>
							<Border
								x:Name="PART_Track"
								Margin="0"
								BorderThickness="0"
								CornerRadius="3" />
							<Border
								x:Name="PART_Indicator"
								Margin="0"
								BorderThickness="0"
								CornerRadius="3"
								HorizontalAlignment="Left"
								Background="{StaticResource ProgressBarTrackBackgroundBrush}"
								ClipToBounds="True">
								<Border
									x:Name="DiagonalDecorator"
									Width="5000">
									<Border.Background>
										<DrawingBrush
											TileMode="Tile"
											Stretch="None"
											Viewbox="0,0,1,1"
											Viewport="0,0,36,34"
											ViewportUnits="Absolute">
											<DrawingBrush.RelativeTransform>
												<TranslateTransform
													X="0"
													Y="0" />
											</DrawingBrush.RelativeTransform>
											<DrawingBrush.Drawing>
												<GeometryDrawing
													Brush="#48C739"
													Geometry="M0,0 18,0 36,34 18,34 Z" />
											</DrawingBrush.Drawing>
										</DrawingBrush>
									</Border.Background>
									<Border.Triggers>
										<EventTrigger
											RoutedEvent="FrameworkElement.Loaded">
											<BeginStoryboard>
												<Storyboard>
													<DoubleAnimation
														Storyboard.TargetProperty="(Border.Background).(DrawingBrush.RelativeTransform).(TranslateTransform.X)"
														From="0"
														To=".36"
														RepeatBehavior="Forever"
														Duration="0:0:18" />
												</Storyboard>
											</BeginStoryboard>
										</EventTrigger>
									</Border.Triggers>
								</Border>
							</Border>
						</Grid>
					</local:ClippingBorder>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>-->


    <Storyboard x:Key="FadeIn" FillBehavior="HoldEnd">
        <!--  set Visibility to Visible  -->
        <ObjectAnimationUsingKeyFrames
            BeginTime="0:0:0"
            Storyboard.TargetProperty="Visibility"
            Duration="0:0:0">
            <DiscreteObjectKeyFrame Value="{x:Static Visibility.Visible}" />
        </ObjectAnimationUsingKeyFrames>

        <!--  Animate ScaleX  -->
        <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleX)">
            <EasingDoubleKeyFrame KeyTime="0:0:0.5" Value="1">
                <EasingDoubleKeyFrame.EasingFunction>
                    <CircleEase EasingMode="EaseOut" />
                </EasingDoubleKeyFrame.EasingFunction>
            </EasingDoubleKeyFrame>
        </DoubleAnimationUsingKeyFrames>
        <!--  Animate ScaleY  -->
        <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleY)">
            <EasingDoubleKeyFrame KeyTime="0:0:0.5" Value="1">
                <EasingDoubleKeyFrame.EasingFunction>
                    <CircleEase EasingMode="EaseOut" />
                </EasingDoubleKeyFrame.EasingFunction>
            </EasingDoubleKeyFrame>
        </DoubleAnimationUsingKeyFrames>
    </Storyboard>



    <Storyboard x:Key="FadeOut">
        <!--  Animate ScaleX  -->
        <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleX)">
            <EasingDoubleKeyFrame KeyTime="0:0:0.5" Value="0">
                <EasingDoubleKeyFrame.EasingFunction>
                    <CircleEase EasingMode="EaseOut" />
                </EasingDoubleKeyFrame.EasingFunction>
            </EasingDoubleKeyFrame>
        </DoubleAnimationUsingKeyFrames>
        <!--  Animate ScaleY  -->
        <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleY)">
            <EasingDoubleKeyFrame KeyTime="0:0:0.5" Value="0">
                <EasingDoubleKeyFrame.EasingFunction>
                    <CircleEase EasingMode="EaseOut" />
                </EasingDoubleKeyFrame.EasingFunction>
            </EasingDoubleKeyFrame>
        </DoubleAnimationUsingKeyFrames>
        <!--  set Visibility to Collapsed  -->
        <ObjectAnimationUsingKeyFrames
            BeginTime="0:0:0.5"
            Storyboard.TargetProperty="Visibility"
            Duration="0:0:0">
            <DiscreteObjectKeyFrame Value="{x:Static Visibility.Collapsed}" />
        </ObjectAnimationUsingKeyFrames>
    </Storyboard>


    <Storyboard x:Key="FadeIn2">
        <ObjectAnimationUsingKeyFrames
            BeginTime="0:0:0"
            Storyboard.TargetProperty="Visibility"
            Duration="0:0:0">
            <DiscreteObjectKeyFrame Value="{x:Static Visibility.Visible}" />
        </ObjectAnimationUsingKeyFrames>
        <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="Opacity">
            <EasingDoubleKeyFrame KeyTime="0:0:0.3" Value="1">
                <EasingDoubleKeyFrame.EasingFunction>
                    <CircleEase EasingMode="EaseInOut" />

                </EasingDoubleKeyFrame.EasingFunction>
            </EasingDoubleKeyFrame>
        </DoubleAnimationUsingKeyFrames>
    </Storyboard>

    <Storyboard x:Key="FadeOut2">
        <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="Opacity">
            <EasingDoubleKeyFrame KeyTime="0:0:0.3" Value="0">
                <EasingDoubleKeyFrame.EasingFunction>
                    <ExponentialEase EasingMode="EaseInOut" />
                </EasingDoubleKeyFrame.EasingFunction>
            </EasingDoubleKeyFrame>
        </DoubleAnimationUsingKeyFrames>
        <ObjectAnimationUsingKeyFrames
            BeginTime="0:0:0.3"
            Storyboard.TargetProperty="Visibility"
            Duration="0:0:0">
            <DiscreteObjectKeyFrame Value="{x:Static Visibility.Collapsed}" />
        </ObjectAnimationUsingKeyFrames>
    </Storyboard>

    <!--  ListBox  -->
    <Style TargetType="ListBox">
        <Setter Property="BorderThickness" Value="0" />
        <!--<Style.Resources>
            <Style TargetType="Border">
                <Setter Property="CornerRadius" Value="10"/>
            </Style>
        </Style.Resources>-->
    </Style>

    <!--  ListBox Items  -->
    <Style TargetType="ListBoxItem">
        <Setter Property="Margin" Value="2" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="BorderThickness" Value="0" />
        <Style.Resources>
            <Style TargetType="Border">
                <Setter Property="CornerRadius" Value="5" />
            </Style>
        </Style.Resources>
    </Style>

    <!--  Scrollbar Thumbs  -->
    <Style x:Key="ScrollThumbs" TargetType="{x:Type Thumb}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Thumb}">
                    <Grid x:Name="Grid">
                        <Rectangle
                            Width="Auto"
                            Height="Auto"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Stretch"
                            Fill="Transparent" />
                        <Border
                            x:Name="Rectangle1"
                            Width="Auto"
                            Height="Auto"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Stretch"
                            Background="{TemplateBinding Background}"
                            CornerRadius="5" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="Tag" Value="Horizontal">
                            <Setter TargetName="Rectangle1" Property="Width" Value="Auto" />
                            <Setter TargetName="Rectangle1" Property="Height" Value="7" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  ScrollBars  -->
    <Style x:Key="{x:Type ScrollBar}" TargetType="{x:Type ScrollBar}">
        <Setter Property="Stylus.IsFlicksEnabled" Value="true" />
        <Setter Property="Foreground" Value="{StaticResource PrimaryBackgroundColor}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Width" Value="8" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ScrollBar}">
                    <Grid
                        x:Name="GridRoot"
                        Width="8"
                        Background="{TemplateBinding Background}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="0.00001*" />
                        </Grid.RowDefinitions>

                        <Track
                            x:Name="PART_Track"
                            Grid.Row="0"
                            Focusable="false"
                            IsDirectionReversed="true">
                            <Track.Thumb>
                                <Thumb
                                    x:Name="Thumb"
                                    Background="{TemplateBinding Foreground}"
                                    Style="{DynamicResource ScrollThumbs}" />
                            </Track.Thumb>
                            <Track.IncreaseRepeatButton>
                                <RepeatButton
                                    x:Name="PageUp"
                                    Command="ScrollBar.PageDownCommand"
                                    Focusable="false"
                                    Opacity="0" />
                            </Track.IncreaseRepeatButton>
                            <Track.DecreaseRepeatButton>
                                <RepeatButton
                                    x:Name="PageDown"
                                    Command="ScrollBar.PageUpCommand"
                                    Focusable="false"
                                    Opacity="0" />
                            </Track.DecreaseRepeatButton>
                        </Track>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger SourceName="Thumb" Property="IsMouseOver" Value="true">
                            <Setter TargetName="Thumb" Property="Background" Value="{DynamicResource ButtonSelectBrush}" />
                        </Trigger>

                        <Trigger SourceName="Thumb" Property="IsDragging" Value="true">
                            <Setter TargetName="Thumb" Property="Background" Value="{DynamicResource DarkBrush}" />
                        </Trigger>

                        <Trigger Property="IsEnabled" Value="false">
                            <Setter TargetName="Thumb" Property="Visibility" Value="Collapsed" />
                        </Trigger>

                        <Trigger Property="Orientation" Value="Horizontal">
                            <Setter TargetName="GridRoot" Property="LayoutTransform">
                                <Setter.Value>
                                    <RotateTransform Angle="-90" />
                                </Setter.Value>
                            </Setter>
                            <Setter TargetName="PART_Track" Property="LayoutTransform">
                                <Setter.Value>
                                    <RotateTransform Angle="-90" />
                                </Setter.Value>
                            </Setter>
                            <Setter Property="Width" Value="Auto" />
                            <Setter TargetName="Thumb" Property="Tag" Value="Horizontal" />
                            <Setter TargetName="PageDown" Property="Command" Value="ScrollBar.PageLeftCommand" />
                            <Setter TargetName="PageUp" Property="Command" Value="ScrollBar.PageRightCommand" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  Expander  -->
    <Style
        x:Key="Expander_Style"
        BasedOn="{StaticResource MaterialDesignExpander}"
        TargetType="Expander">
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="Margin" Value="0,5,0,0" />
        <Setter Property="Height" Value="50" />
        <Setter Property="Background" Value="{StaticResource LightBackgroundColor}" />
    </Style>

</ResourceDictionary>