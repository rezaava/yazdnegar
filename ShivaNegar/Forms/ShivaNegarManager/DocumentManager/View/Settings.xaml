<UserControl
	x:Class="ShivaNegar.Forms.ShivaNegarManager.DocumentManager.View.Settings"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	d:DesignHeight="450"
	d:DesignWidth="800"
	FlowDirection="RightToLeft"
	FontFamily="Vazirmatn"
	mc:Ignorable="d">

	<UserControl.Resources>
		<ResourceDictionary>
			<ResourceDictionary.MergedDictionaries>
				<ResourceDictionary Source="/ShivaNegar;component/Forms/Styles/MainDictionary.xaml" />
			</ResourceDictionary.MergedDictionaries>
		</ResourceDictionary>
	</UserControl.Resources>

	<Grid>
		<Border Background="{StaticResource LightBackgroundColor}" CornerRadius="10" />

		<StackPanel>
			<Grid Margin="10">
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="Auto" />
					<ColumnDefinition Width="*" />
				</Grid.ColumnDefinitions>

				<TextBox
					x:Name="txtWorkspace"
					Grid.Column="1"
					Margin="5"
					VerticalAlignment="Top"
					materialDesign:HintAssist.Background="White"
					materialDesign:HintAssist.FloatingScale="1"
					materialDesign:HintAssist.Hint="تغییر پوشه کاری"
					materialDesign:TextFieldAssist.HasLeadingIcon="True"
					materialDesign:TextFieldAssist.LeadingIcon="FolderEdit"
					FlowDirection="LeftToRight"
					IsReadOnly="True"
					Style="{StaticResource MaterialDesignOutlinedTextBox}" />

				<Button
					x:Name="btnChangeWorkspace"
					Grid.Column="0"
					Width="50"
					HorizontalAlignment="Right"
					VerticalAlignment="Stretch"
					Content="..."
					Foreground="{StaticResource LightButtonTextColor}"
					ToolTip="تغییر آدرس Workspace" />
			</Grid>
		</StackPanel>
	</Grid>
</UserControl>
